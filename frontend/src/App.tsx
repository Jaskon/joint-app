import React from 'react';
import Router from './router/Router';

const App = () => {
    return (
        <React.StrictMode>
            <Router />
        </React.StrictMode>
    );
};

export default App;
